{
  "name": "alfred-ai",
  "version": "5.6.1",
  "type": "module",
  "description": "Alfred - Autonomous agentic CLI coding assistant. Takes natural language instructions and executes them using LLM + tools.",
  "main": "alfred-cli.js",
  "bin": {
    "alfred": "./alfred-cli.js"
  },
  "scripts": {
    "test": "node -e \"const { AuthenticationManager } = require('./auth-manager.js'); const auth = new AuthenticationManager(); auth.getAuthentication().then(token => { console.log('SUCCESS: Token found:', token?.substring(0, 50)); }).catch(err => { console.log('ERROR:', err.message); });\"",
    "start": "node alfred-cli.js",
    "test:gpu": "source /etc/profile.d/gpu-acceleration.sh && playwright test",
    "playwright:gpu": "source /etc/profile.d/gpu-acceleration.sh && playwright"
  },
  "keywords": [
    "ai",
    "cli",
    "assistant",
    "automation",
    "coding"
  ],
  "author": "Alfred AI",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.67.0",
    "express": "^5.1.0",
    "fast-glob": "^3.3.2",
    "playwright": "^1.56.1"
  }
}
