# Alfred AI Authentication
# Get your API key from: https://console.anthropic.com/settings/keys

# Option 1: Set API key in environment (recommended for development)
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Option 2: Alternative environment variable name
# ANTHROPIC_AUTH_TOKEN=sk-ant-your-api-key-here

# Usage:
# 1. Copy this file to .env
# 2. Replace the placeholder with your actual API key
# 3. Load it: source <(grep -v '^#' .env | sed 's/^/export /')
# 4. Or add to your shell profile: echo 'export ANTHROPIC_API_KEY=sk-ant-...' >> ~/.bashrc
